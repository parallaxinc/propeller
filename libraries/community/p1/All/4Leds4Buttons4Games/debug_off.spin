{{
This object has the same methods as debug, but the methods do nothing.
To send messages to the serial terminal, use object "debug". To turn debugging off
without commenting out any of the debug statements use object "debug_off"
}}
pub init
pub print (lable, val)
pub print2(lable1, val1, lable2, val2)
pub print3Dec(val1, val2, val3)     